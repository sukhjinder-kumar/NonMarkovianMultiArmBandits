# Non-Markovian Multi Arm Bandit

## Problem Definition


I was using alpha/beta + c/(alpha+beta) as the heuristic - very very bad (linear!)
But alpha/(alpha+beta) + c/(alpha+beta) as the heuristic - Beats UCB!